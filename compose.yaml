services:
  mysql:
    image: mysql:5.7
    container_name: mysql
    ports:
      - 13306:3306
    volumes:
      - "/etc/localtime:/etc/localtime"
    environment:
      MYSQL_ROOT_PASSWORD: qin1002.
    restart: always
